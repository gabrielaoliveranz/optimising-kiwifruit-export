// Copy this file to config.local.js and replace with your real Mapbox public token.
// config.local.js is gitignored — never commit your real token.
window.APO_CONFIG = {
  mapboxToken: 'pk.YOUR_MAPBOX_PUBLIC_TOKEN_HERE'
};
